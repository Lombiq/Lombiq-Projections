namespace Lombiq.Projections.Constants
{
    public static class FeatureNames
    {
        private const string Prefix = "Lombiq.Projections.";

        public const string Fields = Prefix + nameof(Fields);
        public const string Taxonomies = Prefix + nameof(Taxonomies);
    }
}